import { PageHeader } from "antd";

const Header = () => {
  return (
    <PageHeader
      className="site-page-header"
    //   onBack={() => null}
      title="Feed App"
      subTitle="React Feed App"
    />
  );
};

export default Header;
